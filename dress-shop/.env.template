# Copy this file to `.env` (gitignored) and fill in your own values.
# The chatbot reads it at startup — nothing is ever committed.

# Your Anthropic API key (https://platform.claude.com) — required for `npm run chat`
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the model (default: claude-opus-4-8)
# ANTHROPIC_MODEL=claude-opus-4-8
